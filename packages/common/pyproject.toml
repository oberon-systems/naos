[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "naos-common"
version = "0.0.0"
requires-python = ">=3.12"

[tool.setuptools.package-data]
naos_common = ["py.typed"]
